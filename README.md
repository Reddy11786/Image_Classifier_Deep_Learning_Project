# cnnClassifier

Image classification problem

# Workflow
update config.yaml,
update params.yaml,
update the entity,
update configuration manager in src/config.yaml,
update the component,
update the pipeline.
